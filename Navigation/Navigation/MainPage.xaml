<Page
    x:Class="Navigation.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Navigation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Margin="20,0,0,0">
        <StackPanel Orientation="Horizontal" Margin="0,20,0,20">
            <Button Content="Home" x:Name="HomeButton" Click="HomeButton_Click" Margin="0,0,10,0"/>
            <Button Content="Back" x:Name="BackButton" Click="BackButton_Click" Margin="0,0,10,0"/>
            <Button Content="Next" x:Name="NextButton" Click="NextButton_Click" Margin="0,0,10,0"/>
            <Button Content="Navigate" x:Name="NavigateButton" Click="NavigateButton_Click" Margin="0,0,10,0"/>
        </StackPanel>
        <Frame x:Name="MyFrame" MinHeight="300">

        </Frame>
    </StackPanel>

</Page>
